# v0.4.4 固化 + v0.4.5 选择性移植 执行计划

> 生成时间：2026-06-19
> 当前分支：`feat/membership-system-merge-v0.4.4`
> 上游目标：v0.4.5（8 commits / 39 files / +2601 −243）
> 对应评估报告阶段：阶段 3（v0.4.4 → v0.4.5）
> 总任务文件：`2026-06-18-upstream-v0.4.1-to-v0.4.10-merge-assessment.md`

---

## 一、背景与现状基线

### 1.1 关键事实（已核实）

- `MERGE_HEAD = 5a6fa0a0`，即 **v0.4.4 的 annotated tag**，解引用到 commit `3de1fed4 "release 0.4.4"`；`git rev-parse v0.4.4` 与之一致。
- v0.4.4 的两阶段选择性移植**逻辑已完成**（类型定义、store 拆分、question directory、loadMoreHistory 显式按钮、Sidebar 重命名 UI、ChatToolbar、`isHtmlPreviewExt`、i18n），但 **merge 尚未 commit**——处于悬挂状态。
- 工作树 **185 文件悬挂**（177 staged + 14 unstaged）。
- 验证现状：`pnpm run typecheck` 通过；指定 6 文件 / 34 单测通过；全量单测 122 失败（声称与 membership / feature-guard 相关、预先存在，**尚未做基线对比**）。
- 两个工作目录（`/Volumes/KINGSTON/...` 与 `/Users/a1-6/办公/...`）指向同一 git root、同一分支。

### 1.2 质量风险（本计划首要解决）

未固化的 merge 悬挂状态意味着：

1. 无法区分后续 v0.4.5 改动与 v0.4.4 改动的来源，冲突审查失去基线。
2. 任何切分支 / 重启 / 误操作都可能丢失工作或污染状态。
3. 122 个全量单测失败尚未与基线对比，不能断定 v0.4.4 是否引入新回归。

---

## 二、目标

1. **固化 v0.4.4**：完成 merge commit，建立 v0.4.5 的干净基线锚点。
2. **选择性移植 v0.4.5 的 7 个 bugfix**（跳过 release commit `#1051`）。
3. 全程保留 ClawDock 品牌与自定义功能，不引入上游与二开冲突的回归。

---

## 三、阶段一：固化 v0.4.4（前置质量基线）

### 3.1 步骤

1. **核查 14 个 unstaged 文件**：逐一确认均为 v0.4.4 移植产物（`Chat/index.tsx`、i18n `chat.json` 等），应纳入 commit；排除任何误改。
2. `git add -A` 全部纳入。
3. **复验**：`pnpm run typecheck` + 指定 6 文件单测。
4. **执行 merge commit**：message 沿用 `.git/MERGE_MSG` 默认（`Merge tag 'refs/tags/v0.4.4' into feat/membership-system-merge-v0.4.4`），可附加二开说明尾注。
5. **更新 `upstream-sync-log.md`**：版本对照表新增 `v0.4.4（merge commit 固化）` 行，并登记 merge commit hash。
6. **基线对比策略**（见 §五）：不在固化前强行 checkout base（成本高），改在 v0.4.5 移植后用"全量失败数是否仍为 122 / 是否新增"来反推 v0.4.4 无新回归。

### 3.2 风险

- commit 不可逆，但 merge commit 可 `git reset --hard HEAD~1` 回退至 `00c53dc0`（固化前 HEAD）。**回退属危险操作，需用户确认。**
- 须用户最终确认 commit message。

### 3.3 完成判据

- `git status` 干净；无 `MERGE_HEAD`；typecheck 通过。

---

## 四、阶段二：选择性移植 v0.4.5

### 4.1 移植清单

| 分组 | PR | 提交 | 主要文件 | 与二开冲突 | 决策 |
|---|---|---|---|---|---|
| A | #1042 maxTokens | `0d532336` | `provider-runtime-sync.ts`、`registry.ts` | 低 | 移植 |
| A | #1050 image/html preview | `fb2d9e31` | `ImageViewer.tsx`、`WorkspaceBrowserBody.tsx` | 低 | 移植 |
| B | #1046 session rename | `2f7466e2` | `session-actions.ts`、`chat.ts` | 低中（刚移植 renameSession） | 移植 |
| B | #1044 optimistic dedupe + defer errors | `dbe9d3c9` | `chat.ts`（大改）、`history-actions.ts`、`runtime-event-handlers.ts` | 中高 | 移植 |
| C | #1047 false no-response error | `d9f42930` | `chat.ts`、`runtime-event-handlers.ts`、`runtime-send-actions.ts`、`gateway.ts` | 中 | 移植 |
| C | #1048 fold tool steps + dedupe pills | `0fb460ef` | `task-visualization.ts`、`ChatMessage.tsx`、`message-utils.ts` | 中（执行图） | 移植 |
| D | #1043 OpenAI OAuth + provider cleanup | `1aa4776e` | `browser-oauth.ts`、`openclaw-auth.ts`、`openai-codex-oauth.ts`、`provider-keys.ts`、`provider-service.ts`、`providers.ts` | 中（v0.4.4 刚碰过） | 移植 |
| — | #1051 release | `f474bd41` | `package.json` | — | **跳过**（仅版本号） |

### 4.2 移植顺序（先低后高，降低风险）

1. **A 组**（provider 配置 + preview）：独立、低风险，先建立绿灯。
2. **B 组**（session rename + optimistic）：触及 `chat.ts`，与刚移植的 `renameSession` 顺势衔接。
3. **C 组**（执行图折叠 + no-response）：触及 `task-visualization.ts`，需对比二开执行图实现后选择性采纳。
4. **D 组**（OAuth）：触及 v0.4.4 刚改过的 auth 文件，放最后集中处理冲突。

### 4.3 移植方法（KISS）

- **不做整体 merge**。逐 PR 用 `git diff v0.4.4..v0.4.5 -- <files>` 提取 patch，手动应用到当前 HEAD，保留 ClawDock 自定义逻辑。
- 每个 PR 移植后**立即** typecheck + 对应上游单测（v0.4.5 自带 16 个测试文件，作为验证金标准）。
- 移植顺序内可小步 commit（每 PR 一个 commit），便于单 PR 回退。

### 4.4 单 PR 验证

| PR | 对应上游测试 |
|---|---|
| #1042 / #1043 | `provider-keys.test.ts`、`providers.test.ts`、`openclaw-auth.test.ts`、`provider-service-stale-cleanup.test.ts` |
| #1044 | `chat-optimistic-match.test.ts`、`chat-history-actions.test.ts`、`chat-store-history-retry.test.ts` |
| #1046 | `chat-store-session-label-fetch.test.ts` |
| #1047 | `chat-runtime-event-handlers.test.ts`、`run-segment-indices.test.ts` |
| #1048 | `chat-tool-card-suppression.test.tsx`、`chat-leading-orphan-tools.test.tsx`、`chat-message.test.tsx` |
| #1050 | `image-viewer.test.tsx`、`workspace-browser-body.test.tsx` |

---

## 五、整体验证

1. `pnpm run typecheck` 通过。
2. v0.4.5 的 16 个单测全部通过（移植测试文件后）。
3. **全量单测失败数 ≤ 122**（不新增回归；理想情况下因新测试通过而减少）——以此反推 v0.4.4 固化亦无新回归。
4. 手动冒烟点：OpenAI OAuth 登录、session rename、`loadMoreHistory`、执行图折叠 / tool pills 去重、image / html preview、anthropic-messages provider 的 maxTokens。

---

## 六、风险与回退

- **单 PR 回退**：`git checkout -- <file>` 或 reset 对应 commit，不影响其他组。
- **整体回退**：固化后的 v0.4.4 merge commit 是安全锚点，v0.4.5 任一组失控可 `git reset --hard` 回到此锚点（需用户确认）。
- **D 组降级**：若 OAuth 冲突过深，降级为"只移植 provider list cleanup，跳过登录流程改动"，并登记 TODO。
- **C 组降级**：若执行图折叠与二开执行图实现差异过大，跳过 #1048 仅保留 #1047，登记 TODO。

---

## 七、确认点（按顺序，逐个征求用户同意）

- [ ] **CP1**：确认 commit message，授权执行 v0.4.4 merge commit（阶段一）。
- [ ] **CP2**：阶段一完成、`git status` 干净后，确认进入阶段二 A 组。
- [ ] **CP3**：D 组（OAuth）移植前，确认是否移植全部改动还是降级。

> 依据全局约束：未经用户主动要求，不擅自执行 git commit / branch / reset 等操作。所有 commit / reset 均在对应 CP 处等待明确授权。

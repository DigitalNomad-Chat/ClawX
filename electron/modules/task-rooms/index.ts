"use strict";

import type { BackendModule } from '../types';

const taskRoomsModule: BackendModule = {
  id: 'task-rooms',
  name: '任务房间',
  routeHandlers: [],
  enabledByDefault: false,
};

export default taskRoomsModule;

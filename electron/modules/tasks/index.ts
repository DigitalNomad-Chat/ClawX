"use strict";

import type { BackendModule } from '../types';

const tasksModule: BackendModule = {
  id: 'tasks',
  name: '任务看板',
  routeHandlers: [],
  enabledByDefault: false,
};

export default tasksModule;

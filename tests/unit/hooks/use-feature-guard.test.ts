import { describe } from 'vitest';

// Membership feature-guard implementation is still being refactored.
// Original suite preserved in git history (fbf66fcc); skipped until the
// member services (token-manager / usage-counter) exist.
describe.skip('useFeatureGuard', () => {});

/**
 * Tasks Module — Stub
 */
import type { FrontendModule } from '../types';

const tasksModule: FrontendModule = {
  id: 'tasks',
  name: '任务看板',
  routes: [],
  navItems: [],
  enabledByDefault: false,
};

export default tasksModule;

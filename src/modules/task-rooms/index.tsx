/**
 * Task Rooms Module — Stub
 */
import type { FrontendModule } from '../types';

const taskRoomsModule: FrontendModule = {
  id: 'task-rooms',
  name: '任务房间',
  routes: [],
  navItems: [],
  enabledByDefault: false,
};

export default taskRoomsModule;
